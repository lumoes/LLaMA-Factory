### model
model_name_or_path: /root/autodl-tmp/models/llama3-8b
trust_remote_code: true

### method
stage: sft
finetuning_type: ffn_only
do_train: true

### dataset
dataset: math
cutoff_len: 4096
max_samples: 10000
overwrite_cache: true
preprocessing_num_workers: 16
dataloader_num_workers: 4

### output
output_dir: /root/autodl-tmp/saves/math/ffn
logging_steps: 10
save_steps: 1000
plot_loss: true
overwrite_output_dir: true
save_only_model: false
report_to: none  # choices: [none, wandb, tensorboard, swanlab, mlflow]

### train
per_device_train_batch_size: 8   # ✅ 2. 显存够的话改成 2，不够就保持 1
gradient_accumulation_steps: 4    # 保持梯度累积，稳定更新
learning_rate: 5.0e-5            # ✅ 3. 稍微调低一点，为了配合长 Epoch
num_train_epochs: 30.0
lr_scheduler_type: cosine
warmup_ratio: 0.1
bf16: true
ddp_timeout: 180000000
resume_from_checkpoint: null

## eval
eval_dataset: c4_demo
val_size: 0.1
per_device_eval_batch_size: 8
eval_strategy: steps
eval_steps: 500
